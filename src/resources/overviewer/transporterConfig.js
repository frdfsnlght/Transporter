var transporterConfig = {

    // which world is being shown in the map
    world: 'world',
    
    // how often, in seconds, to update the gates on the map    
    refresh: 60,
    
    // where is the gates file the plugin periodicaly saves
    gates: 'transporter/gates.json',
    
    // what image to use for markers on the map
    gateMarkerIcon: 'transporter/gate-marker.png',
    
    // should clicking a marker show a gate info window?
    gateInfo: true,
    
    // what image to use for markers on the map
    gateInfoIcon: 'transporter/gate-info.png'
    
    
};
